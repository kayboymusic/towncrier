import { StubPage } from "@/components/layout/stub-page";

export default function SavedPage() {
  return (
    <StubPage
      title="Saved"
      description="Your bookmarked items. Requires authentication, which is deferred."
      prdSection="§20"
    />
  );
}
