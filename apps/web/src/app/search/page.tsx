import { StubPage } from "@/components/layout/stub-page";

export default function SearchPage() {
  return (
    <StubPage
      title="Search"
      description="Full-text + semantic + hybrid search across the AI &amp; robotics knowledge base."
      prdSection="§6.6 / §18"
    />
  );
}
