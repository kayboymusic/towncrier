{
  "name": "town-crier",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:web": "pnpm --filter @town-crier/web dev",
    "dev:api": "cd apps/api && uv run uvicorn app.main:app --reload --port 8000",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "workers:run-once": "cd apps/workers && uv run python -m workers.cli run-once",
    "workers:seed-companies": "cd apps/workers && uv run python -m workers.cli seed-companies",
    "workers:poll": "cd apps/workers && uv run python -m workers.cli poll"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.3",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=20"
  }
}
